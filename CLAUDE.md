# Правила работы над этим ботом

Свод правил для любой сессии Клода, работающей в этом репозитории.
Читается автоматически при старте.

## Аналитика — по умолчанию, для каждого нового процесса

Каждый новый хендлер/шаг воронки в этом боте по умолчанию снабжается:

- аналитикой (`add_event` — какой шаг/действие произошло);
- важными деталями в уведомлении админу — строго через
  `bot_services/admin_notifications.py`, не напрямую `bot.send_message`.

Не додумывать самому, какие детали "важные" — если неочевидно, спросить.

## Уведомления админу — тихие по умолчанию

Все уведомления администратору (`bot_services/admin_notifications.py`)
по умолчанию идут **без звука** (`disable_notification=True`).

Со звуком (важные, не трогать без явной просьбы):
- `notify_admin_payment_done` — пользователь подтвердил оплату;
- `notify_care_team` — запрос в отдел Заботы.

Новую функцию уведомления по умолчанию делать тихой; со звуком —
только если Лиза явно скажет, что это важно.

## Разделение ролей

- `ADMIN_ID` — владелец бота, доступ к админ-панели (`handlers/admin.py`).
- `NOTIFY_ADMIN_ID` — куда падают уведомления (сейчас это
  `goncharova_help`, 8673829586). Не путать и не объединять — это
  разные роли, менять их независимо друг от друга.

## Известные баги — найдены, не исправлены

Найдены код-ревью 2026-09-05, чинить только по явной просьбе Лизы —
не трогать заодно с другой задачей:

- **`handlers/cinemalogy/frame_navigation.py:24`** — обработчик
  `frame_select` в этом файле мёртвый: тот же `callback_data` раньше
  перехватывает `confirm_frame_router` (порядок `include_router` в
  `handlers/cinemalogy/__init__.py`). Работает только пока порядок
  роутеров не менялся.
- **`bot_services/current_step.py`** — `set_current_step`/`get_current_step`
  вызывают `set_user_parameter`/`get_user_parameter` с неправильными
  именами аргументов (`user_id=`/`key=`/`value=` вместо
  `telegram_id=`/`parameter_name=`/`parameter_value=`). Упадёт с
  `TypeError` при первом же вызове. Сейчас нигде не импортируется —
  функция «вернуть на последний экран» не реализована вообще.
- **`middlewares/activity.py:29`** — `hasattr(event, "text")` всегда
  истинно для `Message`, поэтому не фильтрует нетекстовые сообщения,
  как выглядит по смыслу кода. Событие `"message"` пишется и для
  фото/стикеров с `event_data=None`.
- **`handlers/admin.py:50`** — свои подключения к БД по
  захардкоженному абсолютному пути `/home/botuser/telegram-bot/data/bot.db`
  вместо импорта `DB_PATH` из `database.py` (там путь относительный).
  Разойдутся, если рабочая директория процесса когда-то изменится.
- **`bot_services/database.py:299`** — `get_all_user_parameters()`
  нигде не вызывается, мёртвый код.
