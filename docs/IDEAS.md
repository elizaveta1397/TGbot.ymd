# Идеи и доработки на будущее

Бэклог — не путать с `HANDOFF.md` (статус текущей/последней сессии,
перезаписывается) и с «Известными багами» в `CLAUDE.md` (конкретные
дефекты, найденные код-ревью). Здесь — идеи и доработки, которые
приходят в голову по ходу разработки, но не делаются прямо сейчас:
не забыть и вернуться, когда дойдут руки.

Как вести:
- Новую идею — одной строкой в раздел «Не сделано», дата в скобках.
  Если есть контекст (почему это важно, откуда взялась идея) — коротко
  под строкой.
- Сделали — переносим в «Сделано» с датой реализации (или просто
  удаляем, если разбирать полезно и без архива).
- Не должно дублировать открытые вопросы в `HANDOFF.md` — те про «что
  блокирует прямо сейчас», это — про «было бы неплохо сделать
  когда-нибудь».

## Не сделано

- **Новые сценарии для ноябрьской Синемалогии с кружками** (2026-09-06)
  Кружки — видео-заметки (video_note) в Telegram; сейчас в воронке
  Синемалогии кадры — только фото (`cinemalogy_frame_XX_image`).
- **Аудит безопасности сервера** (2026-09-06)
  Проверить сервер на предмет взлома, закрыть найденные дыры — включая
  подобные уязвимости в самом Telegram-боте (пример уже найденного и
  исправленного за эту сессию — `handlers/start.py` отдавал `file_id`
  любому пользователю без проверки на админа). Отдельно — починить
  overclick: защита от того, что бота можно "закликать" (пользователь
  быстро жмёт кнопку/и много раз подряд) — нужен дебаунс/rate-limit на
  обработчики callback-кнопок, чтобы спам-клики не приводили к
  дублирующимся действиям, аналитике или гонкам состояния.
- **Соответствие 152-ФЗ (обработка персональных данных)** (2026-09-06)
  Бот хранит username/Telegram ID/историю сообщений (`users`,
  `user_events`) — привести обработку персональных данных в
  соответствие требованиям российского законодательства.
- **Голосование за дату следующей Синемалогии** (2026-09-06)
  Новый сценарий в боте: пользователи голосуют за дату; результаты
  голосования — в дашборд «Проявка» (или отдельная вкладка/секция).
- **Переписать /start и главное меню** (2026-09-06)
  Тексты (и, возможно, структуру) экрана `/start` и кнопок главного
  меню (`handlers/start.py`, `keyboards/main_menu.py`).
- **Переписать тексты под кадрами при выборе** (2026-09-06)
  `CAPTION_TEXT` в `handlers/cinemalogy/frame_navigation.py`
  (используется и в `choose_frame.py`).
- **Статьи кнопками в экране выбора/результата кадра** (2026-09-06)
  Написать сами статьи. Заодно нашла по ходу: код для этой кнопки уже
  частично существует — `handlers/cinemalogy/result.py` читает
  `cinemalogy_frame_XX_article_button`/`..._article_url` через
  `get_material()` и передаёт их в `result_keyboard(...)`
  (`keyboards/cinemalogy/result.py`), но сама функция эти два
  параметра игнорирует — кнопка в клавиатуру не добавлена. Когда
  будем делать — не только контент, но и подключить кнопку в
  `result_keyboard()`.

## Сделано

_(пока пусто)_
